sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t flachens/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true flachens/react-test npm test

after_success:
  - docker build -t flachens/multi-client ./client
  - docker build -t flachens/multi-nginx ./nginx
  - docker build -t flachens/multi-server ./server
  - docker build -t flachens/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push flachens/multi-client
  - docker push flachens/multi-nginx
  - docker push flachens/multi-server
  - docker push flachens/multi-worker